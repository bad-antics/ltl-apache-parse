// examples/demo.ltl — runnable demo for ltl_apache_parse

import std::io
use ltl_apache_parse as lib

fn main() {
    io::println("ltl_apache_parse demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}